{
  "name": "grounded-journal",
  "private": true,
  "version": "0.1.0",
  "description": "Knowledge-grounded journaling app (Cloud Run AI Challenge entry).",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/**/*.test.js",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-admin": "^14.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
